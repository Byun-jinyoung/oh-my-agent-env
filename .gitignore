node_modules/
.DS_Store
*.log
.env
.serena/
.code-review-graph/
graphify-out/
.claude/settings.local.json

# Python bytecode (generated when skills' scripts run)
__pycache__/
*.pyc
*.pyo

# codex-mcp / gemini-mcp runtime logs (per-machine, never synced)
.codex-gemini-mcp/

# Machine-specific local snapshots — generated per machine, never synced
local/
skills/slurm-hpc/references/cluster.generated.md

# oh-my-claudecode runtime state (per-machine: project memory, sessions, state)
.omc/

# oh-my-agent (oma) generated outputs — regenerated by `setup.sh oma`;
# only templates/oma/oma-config.yaml is tracked. (.serena/ already ignored above.)
.agents/
.mcp.json
.claude/skills/
.claude/hooks/
.claude/agents/
.claude/rules/
.antigravitycli/
.migration-backup/

# oh-my-agent runtime (local artifacts — do not commit)
.agents/results/
.agents/state/
.agents/backup/

# GJC local state (session/token logs, per-machine) — keep only tracked config
.gjc/*
!.gjc/config.yml

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Timestamped backup files (per-machine, never synced)
*.bak-*

# oh-my-omo runtime evidence (local artifacts — do not commit)
.omo/evidence/
